# Payroll V6 — 姓名别名、退休字段回填与固定工资独立计税

## 1. 员工姓名别名升级

- `Ashan Employee Salary Profile` 新增结构化子表 `姓名别名（可多个）`。
- 每个别名可记录说明，支持历史错别字、曾用字、外部称谓等。
- 旧字段 `external_name_aliases` 保留为隐藏兼容镜像，现有导入器和历史接口无需一次性重写。
- 同一公司内别名必须唯一，且不能与其他员工正式姓名冲突，避免外部工资导入歧义。
- 工作台员工列表直接显示已登记别名，搜索也支持别名。
- 导入匹配顺序保持：工号 > 正式姓名 > 明确别名 > 唯一一字差异。明确别名优先于模糊匹配。

### 本次迁移自动写入

- A0016 张引弟：`张引娣`
- A0003 刘海锋：`刘海峰`
- Z0005 徐凤云：`徐经理`

这些值只在迁移阶段初始化。部署后由员工母表维护，不在运行时姓名匹配代码中写死。

## 2. 年龄与退休字段可见性

- 员工 DocType 重新分区为：身份与年龄、退休政策、人事与用工、薪酬长期参数、社保公积金、专项附加扣除、银行卡/别名/备注。
- `年龄`、`退休年龄(原)`、`退休年龄(延)`在原生列表/工作台中保持可见。
- 新迁移会对现有员工重新执行身份证/证件类型与退休政策校验，回填年龄和可确定的退休字段。
- 历史外籍证件若旧版本曾被默认识别为中国身份证，会在迁移时纠正为护照，避免校验失败。
- 女职工 50/55 岁类别仍要求人事确认，不根据岗位名称强行猜测。

## 3. 固定工资不依赖外部工资表

固定工资是员工母表的权威月度薪酬输入，不属于外部车间实发表。

### 删除外部工资表

删除 Tab 2 外部实发表时：

1. 清除外部工资来源和外部月度明细；
2. 自动从员工母表恢复 `fixed_salary > 0` 的员工；
3. 立即重新计算该员工社保、公积金、累计个税和实发；
4. Tab 5 个税台账和 Tab 6 综合结算继续保留这些人员；
5. Tab 2 仍保持“待上传”，母表固定工资不会被误认为车间外部实发。

### 从未上传外部工资表

Tab 5 个税申报台账增加只读兜底：即使该月从未创建外部工资结算，符合个税台账规则且 `fixed_salary > 0` 的员工也会按母表参数即时计算并显示。该读取过程不创建数据库结算单。

临时工/零工仍按现有业务规则不进入个税申报台账；返聘类继续参与累计预扣。

## 4. 部署

必须执行：

```bash
bench --site <site> migrate
bench --site <site> clear-cache
bench build --app ashan_cn_procurement
bench restart
```

`migrate` 会创建姓名别名子表、运行别名初始化以及年龄/退休字段回填。`build + clear-cache` 用于确保浏览器加载 V6 工作台界面。
