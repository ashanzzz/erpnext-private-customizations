# ERPNext 16 全局开发哲学与用户核心中心思想总纲 (Supreme Core Mental Model & Global Philosophy)

> **⚠️ 核心最高原则**：本项目的所有开发、重构、UI 设计与前后端架构，必须无条件深度贯彻本总纲。
> **💡 动态核心思想**：**所有铁律必须完全动态自适应，绝不死板硬编码！** 人数、金额、人员结构、分类比例、账期月份与凭证数据，必须根据实际业务发生与上传文件**动态计算、动态核验、动态呈现**！文档中出现的数字（如 26人、25人、19人等）仅为当前账期实例，系统必须在任意人数、任意企业、任意月份下具备完全自适应的动态处理能力！

---

## 🌟 用户的四大核心中心思想支柱 (The 4 Pillars of Core Philosophy)

### 🏛️ 第一支柱：动态业务权责边界纯净化与财务凭证严肃性 (Dynamic Financial Integrity & Boundary Purity)

1. **业务权责边界绝对纯净化与动态过滤**：
   - 每个模块、表格严格只承载其权责范围内的纯净数据，绝不概念混淆或强塞异构数据；
   - **车间外部实发表 (Tab 2 · 24列)**：严格只动态包含当期在车间实际出勤与发薪的员工集合（以车间实发表为准，如 $N_{workshop}$ 人），非车间人员（如外籍工、高管等非车间出勤者）即便在母表在册且录入了基础/固定薪酬，系统也会自动动态识别并隔离过滤，绝不混入车间实发表；
   - **全员母表底册 (Tab 1)**：完整承载全公司当期全部在册员工（$N_{total}$ 人）；
   - **法定个税申报 (Tab 5) 与月度薪酬综合结算 (Tab 6)**：完整动态汇算全公司全部在册员工（$N_{total}$ 人），保障企业法定纳税与综合用工支出的一致合规。
   - **外籍工与直接录入薪资人员全量参算**：外籍工直接输入的税后工资（如 ¥28,000）与 7 项专项附加扣除（如租金 ¥1,500），100% 自动参与个税闭式累计预扣倒推与企业总用工成本综合核定。

2. **在册用工结构与纯净分类体系 (5大标准分类与0工资动态感知)**：
   - 彻底解耦五险一金绑定标签，按法定与用工性质纯净划分为 5 大类：
     - `🛡️ 正式工: 18 人` *(若有未设薪者动态显示：其中 X人 0工资)*；
     - `👴 退休返聘人员: 5 人` *(退休返聘/达龄留用，若有未设薪者动态显示：其中 X人 0工资)*；
     - `⏱️ 临时工: 1 人` *(短期计时/计件/零工，若有未设薪者动态显示：其中 X人 0工资)*；
     - `🏷️ 其他类型员工: 1 人` *(涵盖 外籍工、其他-外籍工、其他-管理、其他-正式工、其他-返聘工等特殊在册人员)*；
     - `🚪 本月离职人员: 1 人` *(当期离职/正常发薪个税/次月减员)*；
   - 任务中枢第 1 步副标题动态呈现：`系统计薪 {N_sys}人 ｜ 外部实发计薪 {N_ext}人 ｜ 0工资 {N_zero}人`。

3. **个税 VBA 1:1 精准对齐与闭式反推算法 (VBA 1:1 Tax Engine)**：
   - 核心反推函数 `derive_gross_from_net_vba()`：根据 7 级累计预扣闭式反推算法，与 Excel VBA 算法达到 100% 吻合、0.00 元误差；
   - **动态基本减除费用**：按员工在申报周期内的实际历史记录月份动态累计（5,000 元/月），严禁按自然月份机械乘以 5,000；
   - **7 项专项附加扣除完整贯通**：子女教育、继续教育、大病医疗、住房贷款利息、住房租金、赡养老人、3岁以下婴幼儿照护；
   - **用工性质法定税表边界**：
     - `临时工/零工`：依法不进入累计预扣税表（税额为 0）；
     - `返聘工/退休返聘/其他-返聘工/外籍工`：依法全量纳入累计预扣税表。
   - **独立个税参数设置**：个税起征点与周期设置与五险一金弹窗彻底解耦，内嵌法定 7 级预扣税率只读对照表。

4. **原始凭证神圣不可篡改与交付纯净源文件**：
   - **原始凭证无损归档**：车间发来的原始实发表（.xlsx/.xls）、税务局社保申报表（PDF）、公积金凭证（ZIP/PDF）上传后必须**原汁原味保存原始二进制文件流**，绝不可用系统二次加工表格覆盖；
   - **自动解压与交付纯净源文件**：用户上传 ZIP 压缩包时，系统必须在后台**自动解压**并仅提取内部真正的 PDF 凭证，下载时直接交付解压且规范命名的纯净 PDF，严禁让用户下载杂乱的多层压缩包；
   - **统一规范中文命名法则**：存入系统的凭证必须统一命名为 `{期间月份}_{企业名称}_{凭证类型}_原始凭证.{ext}`；

5. **铁的财务纪律与前置动态强拦截机制**：
   - 前置任务（底册核实、车间实发导入、社保申报核验、公积金凭证核验）未全部齐备前，**绝对禁止最终核定封账**；
   - 点击核定时必须根据当前月份实际任务就绪状态进行**实时动态校验**，缺失条件时弹出清单式警示弹窗，明确指出未完成项；
   - 封账后全局进入纯只读保护，杜绝数据被篡改；反审核必须登记原因并可审计追溯。

6. **薪酬核算月与法定缴费月双时钟（社保、公积金必守）**：
   - `工资核算月 P` 是薪资计算、员工本月例外、快照与重算的唯一业务边界；例如核算 `2026-07`。
   - `实际缴费/凭证所属月` 固定为 `P + 1`；例如 `2026-07` 核算对应 `2026-08` 社保、公积金缴费及凭证。页面、导出表头、文件标题、凭证校验和归档均须明确显示“实际缴费所属期”，不得把两个时钟混为同一月份。
   - 公积金季度规则中的 `1/4/7/10` 是**实际公积金缴费月**规则，必须以 `P + 1` 判断：核算 7 月对应缴费 8 月，不命中季度自动缴纳；核算 6 月对应缴费 7 月，才命中自动缴纳。严禁以工资核算月替代公积金缴费月判断季度规则。
   - 参缴优先级严格为：`本月例外（仅 P 有效）` ＞ `员工长期策略` ＞ `公司自动规则`。常年“固定缴纳”和一次性“强制缴纳”都可使当月参缴，但前者是持续策略，后者必须有本月范围与审计痕迹，二者不得合并统计或混称。
   - 已确认的祺富基线：孟祥山（`A0006`）全年固定缴纳；其他员工跟随公司规则。非排除类型的历史零基数仅在迁移修复时回填为该年度动态配置的公积金最低基数；长期停缴必须显式设置“固定停缴”。返聘、临时/零工、外籍、实习等法定排除类型无论策略或基数如何均不参缴。
   - 公积金“增加/封存清册”表达的是**长期参缴资格变更**，以最近一期实际缴费的计划月材料为基准，而非以季度自动缴纳/停缴开关为基准。当前资格相对基准新增时导出增加清册；删除基数、设为固定停缴或离职等导致资格消失时导出封存清册，封存原因固定为 `4`。没有历史材料时，才保守按入离职日期识别。
   - 社保、公积金台账的操作表默认只展示当期实际缴费人员；完整在册、停缴人数和金额仍须在服务端全量计算并在汇总卡片保留，不能因表格过滤而失真。

---

### 🎨 第二支柱：体系化设计语言、高信息密度与人机工程美学 (Systematic Consistency & Ergonomics)

1. **单层语义化复合表头与极致冻结窗口 (Single-tier Dual-line Freeze Panes)**：
   - **表头单层复合架构**：采用单层 `th` 复合设计（Dual-line Header Badges），上方为逻辑分组小胶囊（9px/600字重），下方为主标题（12px/700字重），彻底杜绝原生多层 `colspan`/`rowspan` 表头在浏览器复杂缩放与横向滚动时造成的冻结列脱轨缺陷；
   - **语义化冻结类体系**：
     - 列 1（序号）：`.qifu-col-sticky-1` (`left: 0; min-width: 44px;`)
     - 列 2（工号）：`.qifu-col-sticky-2` (`left: 44px; min-width: 75px;`)
     - 列 3（姓名）：`.qifu-col-sticky-3` (`left: 119px; min-width: 90px;`)
     - 底部合计行：`.qifu-col-sticky-foot` (`position: sticky; bottom: 0; z-index: 15;`)
   - **冻结列立体投影**：最后一列冻结列右侧配置微立体渐变投影（`box-shadow: 3px 0 6px rgba(0,0,0,0.06)`），滚动层次分明。

2. **动态视口锁定大宽表高度与零垂直滚动条 (Zero Outer Vertical Scroll)**：
   - **动态视口锁定**：通过 `adjust_active_table_height()` 动态计算 `window.innerHeight - topOffsets`，将表格横向滚动条**永久常驻贴合在当前屏幕视口底沿**；
   - **外层网页禁止出现垂直滚动条**：彻底消灭“下拉到底找滑轮、拖到右边再拉回顶上看姓名”的反人类体验；
   - **双向联动顶部辅助滚动条**：宽表上方挂载 `sync_dual_scrollbars()` 顶部滑轮，左右滚动实时双向同步。

3. **务实、高效、核心决策数据一目了然**：
   - 卡片等宽排版、统一高度、字体字号阶梯一致；
   - 界面一目了然呈现核心决策数据：税前应发、税后实发、社保公积金单位承担与个人代扣、**企业综合总用工支出成本**；
   - Tab 导航防抖零位移：全 Tab 统一保持 600 字重，预留底边框，激活时仅切变颜色，严禁使用 `transform: translateY()`。

4. **动态视口流式响应与弹性约束哲学 (Fluid & Dynamic Viewport Adaptive Architecture)**：
   - **拒绝死板固定硬编码尺寸**：严禁在容器、弹窗（Modal）、宽表与网格上使用死板的固定 `px` 宽度（如写死固定的 `800px`、`1100px`），必须根据用户浏览器视口的实际尺寸进行**动态流式自适应设计**；
   - **动态视口弹性约束（Fluid Clamp & Minmax）**：
     - 弹窗宽度：统一采用 `width: min(94vw, 1400px); max-width: 95vw; margin: 1.25rem auto;`，在大屏（2K/4K/大显示器）下充分舒展铺满高信息密度，在小屏（笔记本/1080P/分屏）下自动等比收缩并自适应横向/纵向内滚动，绝不产生外部裁切；
     - 弹窗高度：主体内容区配置 `max-height: calc(88vh - 120px); overflow-y: auto;`，保证标题栏、操作栏在视口内常驻锁定；
     - 表格自适应容器：高度采用 `clamp(220px, 48vh, 600px)` 或动态视口绑定，表头固定、内容滚动；
   - **列宽与字段弹性流式分配**：
     - 固定标识列（序号、复选框、状态标签等）配置紧凑的最小安全宽度；
     - 描述、规格、物料名称、用途备注等文本列使用弹性宽度（`flex: 1` 或自适应 `min-width`），随浏览器窗口拉伸自动占据剩余空间；
     - 金额、税率、单价、数量等数值列保持 `white-space: nowrap; font-variant-numeric: tabular-nums;`，等宽靠右永不折行；
   - **响应式栅格与流式卡片**：顶部 KPI 卡片网格、筛选控制栏采用 `grid-template-columns: repeat(auto-fit, minmax(200px, 1fr))` 与自适应 Flex 换行，随浏览器窗口缩放平滑自适应。

---

### ⚙️ 第三支柱：清晰架构、原生优先与代码规范 (Clean Architecture & Code Style)

1. **Python 服务层规范**：
   - 遵循 PEP 8 标准，函数必须具备明确的 docstring 与清晰的入参校验；
   - 正常文档写入优先使用 `doc.save(ignore_permissions=True)` 或 `doc.insert()`，保证完整生命周期；
   - 数据修改类 RPC 强制使用 POST 请求，显式声明 `@frappe.whitelist(methods=["POST"])`；
   - 数据库查询必须显式加 `order_by` 排序，杜绝顺序不确定性；
   - 复杂计算前后使用 Python `ast.parse()` 进行静态语法安全自检。

2. **前端 JavaScript / CSS 规范**：
   - 严禁全局变量污染，统一在页面闭包中管理状态；
   - 金额格式化统一调用 `fmtMoney(val)`（输出 `¥ #,##0.00`），金额单元格统一添加 `.qifu-money-cell` 保持等宽靠右排版；
   - 模态弹窗统一使用标准 `frappe.ui.Dialog` 或体系化 Modal，关闭时安全清理 DOM。

3. **自定义模块权限底线：一个模块只保留管理员 / 操作员两级**：
   - 所有新建或改造中的自定义业务模块必须在 `services/authorization_service.py` 的 `MODULE_ACCESS_MODEL` 中声明唯一角色对，现有规范为：
     - 薪酬：`Payroll Manager` / `Payroll Operator`；
     - 油卡与车辆：`Oil Card Manager` / `Oil Card Operator`；
     - 合规与特种设备：`Compliance Manager` / `Compliance Operator`；
     - 物业与租赁：`Property Manager` / `Property Operator`；
     - 税局发票：`Tax Invoice Manager` / `Tax Invoice Operator`。
   - `System Manager` / `Administrator` 是平台级全局管理身份，不算模块的第三种业务角色；`All`、`Desk User`、`Accounts`、`Purchase`、`Stock`、`Fleet` 等通用角色**不得**因为身份名称相近而自动获得自定义模块入口或数据权限。
   - 操作员承担日常录入、导入、计算、导出与解锁申请；管理员额外承担配置、删除、月度封账和解锁审批。没有明确需求时，宁可收紧到操作员，不可默认给管理员权限。
   - 页面入口（`Page.roles`）、DocType 权限（`DocPerm` / `Custom DocPerm`）和服务端 RPC 必须三层同时收口；菜单可见性从不是安全边界。新增接口必须优先调用 `assert_module_access()`，薪酬和油卡必须分别调用 `assert_payroll_access()`、`assert_oil_ledger_access()`。
   - 凡是带 `company` 的读取、导入、导出、写入、锁定和审批操作，必须在服务端调用 `assert_company_access()`；绝不可只靠前端公司筛选。`ignore_permissions=True` 只能用于通过角色和公司强校验后的最终文档写入，绝不可绕过鉴权。
   - 权限初始化位于 `setup.py`，每次迁移都必须保持收紧结果，不能重新向 `All` / `Desk User` 或通用业务角色放权。旧角色迁移仅允许针对已核实的一对一别名；严禁把大范围的财务、采购、库存角色批量映射为自定义模块管理员。
   - 薪酬关键状态只认统一 `PAYROLL_WORKFLOW_POLICY`：`草稿 → 已计算 → 凭证核验通过 → 已封账 → 解锁申请中 → 已解锁`。角色判断、必填原因和审计字段都从该策略读取，不得在页面或旧接口另写一套宽松规则。

4. **前端设计系统、复用与内联样式零增长**：
   - 全局视觉令牌和共享组件唯一入口为 `public/css/ashan_ui_kit.css`。新页面及改造区域优先复用 `ashan-*` 类：卡片、按钮、状态标签、表头胶囊、提示框、金额单元格、文件标签和弹窗区块；不得再复制颜色、间距、圆角、阴影的字面值。
   - 从现在起，新代码**禁止新增** HTML `style=`。静态视觉必须用统一 class；动态状态必须通过 class 切换、`data-*` 状态或既有组件实现，不能以字符串拼接内联样式。只有在用户明确批准且无法用组件表达时才可例外，并须在同一改动中说明原因。
   - 每次前端交付必须运行 `python scripts/verify_ui_style_governance.py`。该脚本以 `ashan_cn_procurement/ui_style_baseline.json` 为上限：只允许减少，不允许增加。不得为了绕过检查而随意抬高基线；确有例外必须先取得用户明确同意。
   - 旧页遵循“改到哪里就抽哪里”：一次只迁移一个独立、可视觉验收的功能区，保持现有页面像素级外观和交互不变，再逐步消除重复。祺富薪酬页优先提取状态标签、按钮、卡片、表头、弹窗、金额单元格六类基础组件。
   - 祺富等超大页面拆分顺序固定为：`数据请求 → 流程状态 → 导入导出 → 表格渲染 → 弹窗`；每次只拆一个功能区，并保留页面闭包与公开事件契约，禁止一次性重写 5,000 行页面。
   - 修改 `ashan_ui_kit.css` 后必须递增 `hooks.py` 内该资源的版本查询参数，避免 Desk / 反向代理继续使用旧 CSS；页面结构和组件类不应依赖浏览器缓存恰好失效。

5. **生产迁移与验收的额外底线**：
   - `scripts/sync_and_migrate.py` 输出“完成”不等于迁移一定成功。若出现 `bench_migrate.lock`、`LockTimeoutError` 或迁移 Traceback，必须先只读确认不存在迁移进程，再仅删除已确认孤立的那一枚锁，并手动执行 `bench --site site1.local migrate`，以退出码 `0` 为准。
   - 生产环境禁止用 `DocType.save()` 修改标准 DocType 结构。迁移期如需收敛自定义模块权限，只能重建对应的 `DocPerm` / `Custom DocPerm` 权限行，不得修改 Frappe 或 ERPNext 核心源码。
   - 浏览器验收必须检查：目标 CSS / JS 的实际版本已加载、控制台无新增报错、角色入口与服务端权限匹配、关键交互可走通。通过 IP 地址访问 Desk 时如出现 Socket.IO `Invalid origin` 或路由回首页，应作为反向代理 Host / Origin 配置问题单独核验，不能误判为业务页面代码已通过。

6. **环境安全与数据脱敏铁律**：
   - 遵循 `.agents/rules/load_env.md`，**严禁在任何源码或脚本中硬编码密码、Token 或连接串**，统一从 `.env` 读取；
   - 真实员工敏感个人信息（身份证、电话、薪水等）严禁提交至 Git 仓库，统一使用 `*.example.json` 示例模板，真实数据通过 `private/files/` 或环境变量动态挂载；
   - 禁止修改 `apps/frappe` 与 `apps/erpnext` 核心代码，所有业务定制沉淀于 `ashan_cn_procurement`。

---

### 🚀 第四支柱：交付铁律：Playwright 自动化实机验收闭环 (Zero-Guessing Automated Verification Standard)

1. **绝对禁止仅凭模型记忆猜测**：所有功能、接口与 UI 修改必须经过实机代码验证；
2. **热同步与迁移闭环**：任何改动必须执行本地到 Docker 容器热同步与迁移（`python scripts/sync_and_migrate.py`）；
3. **Playwright 实机验证与全景截图**：
   - 编写并运行 Playwright 自动化测试脚本；
   - 真实访问 Desk 界面、测试点击与弹窗交互、排查浏览器 Console 报错与网络请求；
   - 截取完整全景图进行视觉与逻辑自检；
4. **100% 无报错后汇报**：确认 100% 运行无误、控制台 0 报错后才向用户汇报并提交 Git 推送。

---

## 📚 常用指令与自动化工具速查 (Command & Tool Cheatsheet)

| 操作目标 | 命令 / 脚本 | 说明 |
| :--- | :--- | :--- |
| **代码热同步与迁移** | `python scripts/sync_and_migrate.py` | 同步代码到容器、执行 migrate、清除缓存并重启服务 |
| **冻结窗口与大宽表验收**| `python scratch/verify_all_freeze_panes.py` | Playwright 实机测试 68列/15列/社保/公积金/结算表与滚动 |
| **个税功能回归测试** | `python test_china_tax_integration.py` | 验证 7 级预扣税率与 VBA 闭式倒推 1:1 对齐 |
| **代码提交与推送** | `git add -A; git commit -m "..."; git push origin main` | 仅在实机验证 0 报错后执行提交推送 |
