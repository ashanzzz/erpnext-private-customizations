# 个税累计预扣 / VBA 对齐变更

本版本基于当前完整项目源码完成以下变更：

- 修复“个税参数设置”误打开社保费率弹窗的问题。
- 个税参数与社保/公积金参数前端入口、读取接口、保存接口分离。
- 完成个人所得税申报台账 68 列 VBA 同口径输出，并保留前三列冻结及双横向滚动。
- 历史累计基本减除费用改为按员工真实历史记录月份累计。
- 补齐“继续教育”等 7 项专项附加扣除链路。
- `重新核定当月个税` 接入 VBA 同口径累计预扣反推。
- `重新融合核算（按最新母表与基数）` 从旧单月税率算法切换为累计预扣算法，避免两个重算入口口径不一致。
- `临时工/零工` 不进入个税申报台账、个税为 0。
- `返聘工/退休返聘/其他-返聘工`进入个税累计预扣台账。
- 员工类型选项增加 `其他-返聘工`，薪资模式编辑选项与 DocType 统一。
- 历史 2025-10 数据明确标注为历史累计期初快照语义。
- 移除 Git 源码包中的真实员工/历史工资 seed 数据，改为站点 private/files 或环境变量加载。
