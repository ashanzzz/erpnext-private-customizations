# 薪酬工作台：历史数据 Tab 与本轮 Bug 修复

## 本轮功能

- 新增 `🗂️ 7. 历史数据` Tab。
- 全员模式展示当前个税申报周期内：已发薪月份、累计税前收入、累计基本减除费用、累计个人社保/公积金、累计专项附加扣除、累计应纳税所得额、当前预扣率、累计已扣个税、累计税后实发。
- 点击员工姓名可直接穿透到该员工 12 个月申报周期月度轨迹。
- 历史 Tab 支持单独导出 Excel；单人模式导出增加周期累计行。
- 个税台账中的员工姓名穿透链接现已真正接通历史 Tab。
- 历史数据随账期切换和全局刷新同步刷新。
- 历史宽表增加顶部辅助横向滚动条，沿用前三列冻结窗口。

## 业务规则保持

- 临时工 / 零工：不进入个人所得税申报台账和累计预扣历史台账。
- 返聘工 / 退休返聘 / 其他-返聘工：进入个人所得税申报台账与累计预扣计算。
- 员工档案 KPI 的“其他-返聘工”仍按项目既有分类显示在“其他类型员工”，税务参与规则与 UI 用工分类解耦。

## Bug 修复

1. 修复个税台账姓名“点击穿透”只有链接样式、没有事件处理的问题。
2. 修复“撤销离职”事件被重复嵌套绑定，首次点击只会注册事件、第二次点击才生效的问题。
3. 修复“导出标准全量 Excel”按钮仍显示 5 张 Sheet，而服务端实际已经导出 7 张 Sheet 的文案不一致。
4. 修复历史 Tab 在切换月份/点击刷新后不刷新的状态同步问题。
5. 为个税 68 列、历史汇总、单人历史穿透、薪酬 Excel 导出增加薪酬工作台读取权限校验，避免普通无权限账号直接调用敏感 RPC。
6. 修复根目录 `query_db_sidebars.py` 的 Python 字符串引号语法错误，并改为从环境变量组装 MariaDB 调试命令。
7. Excel 浏览器下载完成后释放 Object URL，避免重复导出造成前端内存泄漏。
8. 单人历史 Excel Sheet 名增加 31 字符限制，避免超长姓名导致 openpyxl 工作表标题异常。

## UI 优化

- 7 个业务 Tab 支持窄屏自动换行，降低导航溢出。
- 缩小 Tab 间距与内边距，7 项导航保持紧凑。
- 历史 Tab 使用与现有薪酬工作台一致的 KPI、宽表、冻结列、状态标签设计语言。

## 静态校验

- Python AST：997 个 `.py` 文件，0 个语法错误。
- JSON：103 个 `.json` 文件，0 个解析错误。
- JavaScript：79 个 `.js` 文件，0 个 `node --check` 语法错误。

> 当前环境未连接你的实际 ERPNext/Frappe 站点，`test_china_tax_integration.py` 因本地目标站点未启动而无法完成在线回归；因此本轮不声明已完成 bench/Playwright 实机验收。
