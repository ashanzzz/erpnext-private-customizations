# Payroll Master UI V4

## 目标

本版本收紧工资工作台的数据职责，员工母表只保留长期员工属性，外部工资导入只承载来源事实，月度综合结算负责最终核定和输出。

## 现金面额字段

- 不再把外部工资表中的 100/50/10/5/1 元张数提升为标准工资字段。
- 历史 Excel 中的这些列仍保存在 `source_raw_json`，只用于审计和回溯。
- 月度明细 DocType 删除 `source_cash_100/source_cash_50/source_cash_10/source_cash_5/source_cash_1`。
- 第 6 页全量 Excel 导出仍保留 100/50/10/5/1 元配钞辅助列。
- 配钞张数始终以第 6 页最终实发工资为唯一计算来源，历史 Excel 不可覆盖。

## 员工薪酬档案 UI

首页收敛为四个核心指标：在册员工、档案资料完整、退休事项待处理、本月离职。

人员结构和退休规则改为紧凑标签，不再使用大面积统计卡。

员工列表由原来的宽表收敛为 12 列：

1. 选择
2. 序号
3. 工号
4. 姓名
5. 身份资料
6. 岗位 / 用工
7. 计薪 / 固定项
8. 社保 / 公积金
9. 专项附加扣除
10. 退休信息
11. 档案状态
12. 操作

身份证号默认脱敏。列表不再通过 HTML title 暴露完整证件号码。

员工编辑弹窗按基本身份、人事与用工、退休政策、薪酬长期参数、社保与公积金基数、个税专项附加扣除、导入兼容与备注分区。

公积金批量操作统一保留在第 4 页，不再在员工母表重复出现。

## 第 6 页导出

“导出全量 Excel”按钮明确标注包含配钞辅助列。100/50/10/5/1 元张数只在导出时按最终实发工资计算。

## 兼容策略

现金面额列被定义为 `AUDIT_ONLY_ALIASES`。这使老工资表继续能够完整审计，同时避免旧配钞结果进入标准业务模型。

解析器版本升级为 `QIFU-PAYROLL-SEMANTIC-V3`。
