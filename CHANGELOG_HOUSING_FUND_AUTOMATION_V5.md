# 工资工作台 V5：住房公积金季度自动化与例外管理

## 1. 设计目标

本版把“公积金是否缴纳”和“员工长期公积金基数”彻底分离。

员工薪酬档案只保存长期基数和长期策略。系统在计算具体工资账期时，根据公司年度规则、员工长期策略和本月例外生成当月有效公积金基数。非缴纳月份不会把员工母表基数清零。

默认公司规则如下。

- 自动缴纳月份：1、4、7、10 月。
- 其他月份：自动停缴。
- 默认员工策略：跟随公司规则。
- 长期特殊人员可设置为“固定缴纳”。
- 长期不缴人员可设置为“固定停缴”。
- 临时特殊情况使用“本月例外”，只影响一个账期。

迁移时，现有祺富员工“孟祥山”会初始化为“固定缴纳”。运行时没有把姓名写死在计算引擎中。以后可直接在员工长期策略界面修改或增加其他特殊人员。

## 2. 规则优先级

在员工满足基本业务资格、且长期公积金基数大于 0 的前提下，计算顺序为：

1. 本月例外
2. 员工长期策略
3. 公司自动月份规则

例如：

- 2026-07 是计划缴纳月，普通员工按母表长期基数缴纳。
- 2026-08 是非计划月，普通员工当月有效基数为 0，但母表长期基数保持不变。
- “固定缴纳”员工在 2026-08 仍按母表长期基数缴纳。
- 即使员工为“固定缴纳”，本月例外选择“强制停缴”时，该月仍停缴。
- “固定停缴”员工若本月确需补缴，可使用“强制缴纳”。

返聘、临时、零工、外籍、实习等当前业务边界之外的人员仍不会因为本月例外而被强制纳入。

## 3. UI 调整

“🏛️ 4. 住房公积金台账与配置”成为唯一的月度公积金控制中心。

顶部显示：

- 本月自动规则状态
- 本月参缴人数
- 固定缴纳人数
- 本月例外人数
- 停缴人数
- 单位与个人缴存比例
- 年度最低基数
- 当月缴存总额

操作区分为四类：

- 比例与基数：维护年度单位比例、个人比例、最低基数。
- 自动缴纳规则：维护是否启用自动规则、自动缴纳月份、其他月份处理方式。
- 员工长期策略：批量维护“跟随公司规则 / 固定缴纳 / 固定停缴”。
- 本月例外：维护单个员工当前账期“强制缴纳 / 强制停缴 / 取消例外”。

台账不再只显示参缴人员。工作台会同时显示停缴人员，并显示每个人本月为什么缴或为什么不缴。正式导出仍只导出实际参缴人员。

## 4. 员工母表调整

员工薪酬档案增加“公积金长期策略”。

可选值：

- 跟随公司规则
- 固定缴纳
- 固定停缴

原“公积金基数”明确为“公积金长期基数”。长期策略与长期基数都是员工主数据，不随月份自动改写。

## 5. 后端结构

新增独立服务：

`ashan_cn_procurement/services/housing_fund_policy_service.py`

主要职责：

- 标准化账期
- 解析公司自动缴纳月份
- 计算当月有效公积金基数
- 返回可解释的规则来源和原因
- 管理员工长期策略
- 管理本月例外
- 返回公积金规则与员工结果预览

新增 DocType：

`Ashan Housing Fund Monthly Override`

用于保存一次性月度例外。字段包括公司、工资账期、工号、姓名、处理方式和原因。

直接通过 DocType、API 或后续 AI 自动修改时，后端仍会校验账期、员工归属、处理方式和账期冻结状态。

## 6. 年度设置

`Ashan Insurance Setting` 新增：

- `hf_auto_rule_enabled`
- `hf_contribution_months`
- `hf_off_month_action`

默认值为启用、`1,4,7,10`、`停缴`。

这些规则按年度保存，因此未来可以直接配置下一年度，而不需要修改源码。

## 7. 工资计算一致性

新规则已经接入：

- 月度工资生成
- 空白工资账期初始化
- 外部工资 Excel 导入后的工资融合
- 单员工后台重算
- 公积金台账
- 个税扣除
- 综合结算

冻结账期继续展示当时保存的公积金快照，不使用今天的规则反向改写历史。

异步重算输入指纹也已加入：

- 员工 `housing_fund_policy`
- 公司 `hf_auto_rule_enabled`
- 公司 `hf_contribution_months`
- 公司 `hf_off_month_action`
- 当前账期员工本月例外

因此规则变化不会被后台错误判断为“输入未变化”。

## 8. 旧接口保护

旧版“一键设置最低公积金基数 / 一键清零公积金基数”按钮已经移除。

旧 API `set_qifu_housing_fund_batch` 保留接口名用于兼容检查，但现在会明确拒绝执行，不再修改员工母表长期基数。这样旧脚本即使误调用，也不会把长期基数批量清零。

## 9. 自动化方式

本功能采用“按账期实时派生”的自动化，而不是每天或每月运行定时任务去改员工档案。

优点：

- 选择任意历史或未来账期时，规则都按该账期即时计算。
- 不依赖定时任务是否成功执行。
- 不会因为服务器某一天停机而漏掉月份切换。
- 不修改长期母表基数。
- 冻结历史不会被新规则污染。

## 10. 迁移

部署后执行：

```bash
bench --site <site-name> migrate
bench --site <site-name> clear-cache
bench build --app ashan_cn_procurement
bench restart
```

迁移补丁：

`ashan_cn_procurement.patches.v1_4.initialize_housing_fund_quarter_policy`

会初始化已有员工的长期策略、年度自动规则，并把现有“孟祥山”初始化为固定缴纳。

## 11. 静态验证

本版本已完成：

- 修改 Python 文件编译检查
- 工作台 JavaScript `node --check`
- 新增和修改 DocType JSON 解析检查
- 公积金策略 6 项自动测试
- 1、4、7、10 月自动缴纳验证
- 非计划月自动停缴验证
- 固定缴纳验证
- 固定停缴验证
- 本月例外优先级验证
- 长期基数不被改写验证
- 旧危险 API 保护验证

实际 ERPNext 页面渲染、数据库迁移和后台队列仍需在目标站点部署后做最终验收。
