# 薪资发放 / 个税精简表 / 打印导出升级

日期：2026-08-22

本次以 `祺富人事202607.xlsm` 的《当月发薪工资表》《记账工资表》《记账工资表外籍》为业务参考，保持 ERPNext 服务端数据为唯一计算来源，不复制 VBA 宏代码。

## 1. 个人所得税 17 列财税精简版

- 将 Web 表格“序号”定位列压缩为 34px，并同步重排工号、姓名及后续财税字段宽度。
- Excel 导出中序号列改为紧凑宽度，17 个字段按证件、期间、累计金额、税率/税额的阅读需求重新分配宽度。
- 修正 UI 中将 17 列财税精简版误写为“15列”的标签；Tab 6 原 15 列查看弹窗仍保持其真实 15 列定义。

## 2. 外部实发与发放表（24列）

- 修复前端与服务端字段名称不一致造成的空值/0 值显示风险。
- 修复导出构造器使用旧字段名的问题，使 24 列与《当月发薪工资表》A:X 对齐。
- 保持 24 列主表为权威业务表；新增默认折叠的“发放辅助”抽屉，不污染主表。

### 发放辅助

提供三种辅助视图：

1. **逐人分页**：上一人 / 下一人 / 员工选择；单人核对卡；单人打印；批量逐人 A4 分页打印。
2. **DL 信封**：220 × 110 mm 标准 DL 页面；支持当前员工或全部员工逐人分页打印。
3. **现金点钞**：100 / 50 / 10 / 5 / 1 元张数、现金合计、核定差额；支持页面打印和独立 Excel 导出。

现金拆分逻辑与参考 XLSM Y:AE 辅助列一致。已对参考工作簿中 23 条有实发金额的记录做离线交叉验证，点钞张数及核定差额 23/23 一致。

导出的 24 列工作表继续只打印 A:X；Y:AE 保留为隐藏现金辅助列。其中：

- `AD = 100×Y + 50×Z + 10×AA + 5×AB + AC`
- `AE = ROUND(V,0) - AD`

AD/AE 使用实时 Excel 公式，用户如取消隐藏并调整点钞张数，核定结果会自动更新。

## 3. 月度薪酬综合结算 · 记账工资表

Tab 6 增加直接按钮 **“导出记账工资表”**。

导出严格采用参考 XLSM 的 11 列结构：

`工号 / 姓名 / 基本绩效工资 / 职位补贴 / 房/车补 / 税前工资 / 公积金 / 社保 / 应补/退税额 / 合计扣除 / 税后工资合计`

- 普通/返聘员工写入主《记账工资表》。
- 外籍员工存在时自动新增独立《记账工资表外籍》Sheet。
- 基本绩效工资口径与参考表一致：`税前工资 - 职位补贴 - 房/车补`。

## 4. 代码与交互修复

- 修正 Tab 2 汇总栏字段映射。
- 为旧模态框保留兼容别名，避免老入口显示空白。
- 记账工资数据补充序号兼容字段，但 Excel 导出仍严格保持参考表的 11 列、无序号列结构。
- 导出文件 Object URL 继续按使用后回收，避免页面长时间使用产生浏览器资源泄漏。
- 打印功能采用独立 iframe，不修改业务页面 DOM 或服务器数据。

## 验证边界

本次完成源码静态校验及 XLSM 离线结构/点钞逻辑对照；未在真实 ERPNext/Frappe 站点执行 bench、浏览器或打印机驱动级验收。因此 DL 纸张方向、打印机不可打印边距仍应在实际打印机上做一次实机校准。
